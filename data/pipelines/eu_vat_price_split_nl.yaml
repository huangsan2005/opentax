id: eu_vat_price_split_nl
title: "EU VAT price decomposition (standard rate) — NL"
# Netherlands 21% — structure identical to _de variant
inputs:
  gross_price: {"question": "Gross retail price (EUR, VAT included)?", "required": true}
steps:
  - as: vat_rate
    op: param
    rule: eu/vat/standard_nl
    param: rate
    desc: Standard VAT rate (Netherlands)

  - as: net_price
    op: expr
    desc: "net = gross ÷ (1+rate)"
    expr: gross_price / (1 + vat_rate)

  - as: vat_amount
    op: expr
    desc: "VAT = gross − net"
    expr: gross_price - net_price

  - as: vat_share_pct
    op: expr
    result: true
    desc: "VAT share of gross price (%)"
    expr: vat_amount / gross_price * 100
